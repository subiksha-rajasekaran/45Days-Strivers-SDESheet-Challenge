class Solution {
public:
    int Find(int ind,int target,vector<int>& coins,vector<vector<int>>&dp){
        if(ind==0){
            if(target%coins[0]==0) return target/coins[0];
            return 1e9;
        }
        if(dp[ind][target]!=-1) return dp[ind][target];
        int nottake=Find(ind-1,target,coins,dp);
        int take=1e9;
        if(coins[ind]<=target){
            take=1+Find(ind,target-coins[ind],coins,dp);
        }
        return dp[ind][target]= min(take,nottake);
    }
    int coinChange(vector<int>& coins, int amount) {
        int n=coins.size();
        vector<vector<int>>dp(n,vector<int>(amount+1,-1));
        int res=Find(n-1,amount,coins,dp);
        if(res>=1e9) return -1;
        return res;
        
    }
};
